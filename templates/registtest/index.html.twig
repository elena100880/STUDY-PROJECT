{% extends 'base.html.twig' %}   

{% block stylesheets1 %}
            <link rel="stylesheet" href="/css/regtest.css">
           
{% endblock %}

{% block content %}           

    {{form(form)}}
  
    {% if product is defined %}
       
        {% for item in product %}       
            
            {{item.name}} - {{item.price}}$ ({{item.description}})<br>
    
        {% endfor %}        
 
    {% endif %}
    

{% endblock %}