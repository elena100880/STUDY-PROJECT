{% extends 'base.html.twig' %}   

{% block stylesheets1 %}
            <link rel="stylesheet" href="/css/prodedit.css">
           
{% endblock %}

{% block content %}           

    <h1>CHANGING ITEMS</h1>     

    <span>You are changing the item with id {{id}}:</span><br>
    {{name1}} - {{price1}} ({{description1}}) <br><br>
    
    <span>Enter your changes:</span><br>
    {{form(form)}}
    
    {% if save is defined%}
        
        <span>Saved changes in product with id {{product.id}}:</span><br>
        Name: {{product.name}}<br>
        Price: {{product.price}}<br>
        Description: {{product.description}}<br>

    {% endif %}
        

{% endblock %}
